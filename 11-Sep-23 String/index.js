// Q1

/* 
var city = "I live in hyderabad";
var user = "islam";
for (i = 0; i < city.length; i++) {
    // var extractWord = city.slice(i, i + 5);
    // if (extractWord === "hyder") {
    if (city.slice(i, i + 5) === "hyder") {
        city = city.slice(0, i) + "Islam" + city.slice(i + 5);
        console.log(city)
    }
}


 */

// Q2   Paradium Words

var word = "civic";
for (i = word.length - 1; i >= 0; i--) {
    co
}


// Q3

/* 

var word = "abdullah";
var capital = word[0].toUpperCase() + word.slice(1);
console.log(capital);

*/